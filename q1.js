// set up two variables to test
let str1 = "bofa";
let str2 = "bofa";

// check if equal or not
if (str1 == str2) {
    // if equal, say it's equal
    alert("str1 and str2 are equal");
} else {
    // if not equal, state that too
    alert("str1 and str2 aren't equal");
}